package routes

import (
	// http "net/http"
	// httptest "net/http/httptest"
	"testing"
)

func TestPOSTMove(t *testing.T) {
		// request, _ := http.NewRequest(http.MethodPost, "/move", {body, "body"})
		// response := httptest.NewRecorder()
    //
		// MoveHandler(response, request)
    //
		// got := response.Body.String()
		// want := "Hello, you called \"/move\""
    //
		// if got != want {
		// 	t.Errorf("got %q, want %q", got, "Hello foo")
		// }
}
