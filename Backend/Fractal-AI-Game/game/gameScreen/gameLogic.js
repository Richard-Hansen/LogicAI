/**
 * This function is going to check if any squares have been completed by either
 * the AI or the player.
 */
function gameLogic() {
  /* this.checkSquareTaken will check if a square has been taken */
  this.checkSquareTaken = function(square) {

  }
}
